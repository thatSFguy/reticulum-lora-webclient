# Exported identity files contain private keys — never commit.
reticulum-identity-*.json

# Editor / OS droppings
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node (for the Level-2 test harness; the browser app itself has no build step)
node_modules/
package-lock.json
