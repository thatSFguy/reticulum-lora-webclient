# Exported identity files contain private keys — never commit.
reticulum-identity-*.json

# Editor / OS droppings
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/

# Go — go.sum is regenerated in CI (go mod tidy), not committed
tools/go.sum
